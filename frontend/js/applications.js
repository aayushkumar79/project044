requireLogin();
async function loadApplications(){const box=document.getElementById("applicationList");try{const data=await apiRequest(`/applications/${getStudentId()}`);if(!data.applications.length){box.innerHTML='<div class="card"><h2>No applications yet</h2><p>Visit the internships page and apply to a suitable opportunity.</p><a class="button" href="internships.html">Browse Internships</a></div>';return;}box.innerHTML=data.applications.map(a=>`<div class="card app-card"><h2>${a.title}</h2><p>${a.company}</p><span class="status">${a.status}</span></div>`).join("");}catch(e){box.innerHTML=`<div class="card"><p>${e.message}</p></div>`;}} loadApplications();
